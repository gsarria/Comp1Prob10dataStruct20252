#include <stdio.h>
#include <stdlib.h>
struct par 
{ 
    int v, c; 
};
typedef struct par Par;
int main(void){
    int n; 
    scanf("%d",&n);
    // TODO: leer arreglo, comprimir (valor, cantidad consecutiva), imprimir:
    // k
    // v1 c1 v2 c2 ...
    return 0;
}
